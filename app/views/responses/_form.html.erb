<%= form_with(model: response, local: true) do |form| %>
  <% if response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(response.errors.count, "error") %> prohibited this response from being saved:</h2>

      <ul>
      <% response.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :test_case %>
    <%= form.text_field :test_case %>
  </div>

  <div class="field">
    <%= form.label :example %>
    <%= form.text_field :example %>
  </div>

  <div class="field">
    <%= form.label :responded_ms %>
    <%= form.number_field :responded_ms %>
  </div>

  <div class="field">
    <%= form.label :given_answer %>
    <%= form.text_field :given_answer %>
  </div>

  <div class="field">
    <%= form.label :readable_score %>
    <%= form.number_field :readable_score %>
  </div>

  <div class="field">
    <%= form.label :comments %>
    <%= form.text_area :comments %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
